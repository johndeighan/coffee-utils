# log.test.cielo

import {undef, escapeStr} from '@jdeighan/coffee-utils'
import {arrayToBlock} from '@jdeighan/coffee-utils/block'
import {UnitTester, UnitTesterNoNorm} from '@jdeighan/unit-tester'
import {
	stringify, setStringifier, log, setLogger,
	tamlStringify, orderedStringify,
	} from '@jdeighan/coffee-utils/log'

simple = new UnitTester()

# ---------------------------------------------------------------------------

lLines = undef
setLogger (str) -> lLines.push(str)

# ---------------------------------------------------------------------------

class LogTester extends UnitTesterNoNorm

	transformValue: (lLines) ->
		return arrayToBlock(lLines)

tester = new LogTester()

# ---------------------------------------------------------------------------

(() ->
	lLines = []
	log 'enter myfunc'
	tester.equal {{LINE}}, lLines, """
		enter myfunc
		"""
	)()

# ---------------------------------------------------------------------------

(() ->
	lLines = []
	log 'abc'
	log 'def'
	log 'ghi'
	tester.equal {{LINE}}, lLines, """
		abc
		def
		ghi
		"""
	)()

# ---------------------------------------------------------------------------
# test logging various small objects

(() ->
	lLines = []
	log 'abc'
	log undef, 'name'
	tester.equal {{LINE}}, lLines, """
		abc
		name = undef
		"""
	)()

(() ->
	lLines = []
	log 'abc'
	log 42, 'name'
	tester.equal {{LINE}}, lLines, """
		abc
		name = 42
		"""
	)()

(() ->
	lLines = []
	log 'abc'
	log 'John', 'name'
	tester.equal {{LINE}}, lLines, """
		abc
		name = 'John'
		"""
	)()

(() ->
	lLines = []
	log 'abc'
	log {a: 1, b: 'xyz'}, 'name'
	tester.equal {{LINE}}, lLines, """
		abc
		------------------------------------------
		name:
			---
			a: 1
			b: xyz
		------------------------------------------
		"""
	)()

(() ->
	lLines = []
	log ['a', 42, [1,2]], 'name'
	tester.equal {{LINE}}, lLines, """
		------------------------------------------
		name:
			---
			- a
			- 42
			-
				- 1
				- 2
		------------------------------------------
		"""
	)()

# ---------------------------------------------------------------------------
# test logging various large objects

(() ->
	lLines = []
	log 'abc'
	log """
		This is a rather long bit of
		text which changes
		how it's displayed
		""", 'name'
	tester.equal {{LINE}}, lLines, """
		abc
		name:
		==========================================
		This˳is˳a˳rather˳long˳bit˳of
		text˳which˳changes
		how˳it's˳displayed
		==========================================
		"""
	)()

(() ->
	lLines = []
	log 'abc'
	log {
		fname: 'John',
		lname: 'Deighan',
		age: 68,
		home: 'Blacksburg, VA',
		}, 'name'
	tester.equal {{LINE}}, lLines, """
		abc
		------------------------------------------
		name:
			---
			age: 68
			fname: John
			home: Blacksburg,˳VA
			lname: Deighan
		------------------------------------------
		"""
	)()

(() ->
	lLines = []
	log 'abc'
	log [
		68,
		'a rather long string of text',
		{a:1, b:2}
		], 'name'
	tester.equal {{LINE}}, lLines, """
		abc
		------------------------------------------
		name:
			---
			- 68
			- a˳rather˳long˳string˳of˳text
			-
				a: 1
				b: 2
		------------------------------------------
		"""
	)()

# ---------------------------------------------------------------------------
# test ordered stringifier

(() ->
	setStringifier orderedStringify

	lLines = []
	log 'abc'
	log {
		fname: 'John',
		lname: 'Deighan',
		age: 68,
		home: 'Blacksburg, VA',
		}, 'name'
	tester.equal {{LINE}}, lLines, """
		abc
		------------------------------------------
		name:
			---
			age: 68
			fname: John
			home: Blacksburg,˳VA
			lname: Deighan
		------------------------------------------
		"""

	setStringifier tamlStringify    # reset
	)()

# ---------------------------------------------------------------------------
# test providing a prefix

(() ->
	lLines = []
	log undef, {label: 'name', prefix: '<-->'}
	tester.equal {{LINE}}, lLines, """
		<-->name = undef
		"""
	)()

(() ->
	lLines = []
	log 42, {label: 'name', prefix: '<-->'}
	tester.equal {{LINE}}, lLines, """
		<-->name = 42
		"""
	)()

(() ->
	lLines = []
	log 'John', {label: 'name', prefix: '<-->'}
	tester.equal {{LINE}}, lLines, """
		<-->name = 'John'
		"""
	)()

(() ->
	lLines = []
	log {a: 1, b: 'xyz'}, {label: 'name', prefix: '<-->'}
	tester.equal {{LINE}}, lLines, """
		<-->------------------------------------------
		<-->name:
		<-->	---
		<-->	a: 1
		<-->	b: xyz
		<-->------------------------------------------
		"""
	)()

(() ->
	lLines = []
	log ['a', 42, [1,2]], {label: 'name', prefix: '<-->'}
	tester.equal {{LINE}}, lLines, """
		<-->------------------------------------------
		<-->name:
		<-->	---
		<-->	- a
		<-->	- 42
		<-->	-
		<-->		- 1
		<-->		- 2
		<-->------------------------------------------
		"""
	)()

(() ->
	lLines = []
	log """
		This is a rather long bit of
		text which changes
		how it's displayed
		""", {label: 'name', prefix: '<-->'}
	tester.equal {{LINE}}, lLines, """
		<-->name:
		<-->==========================================
		<-->This˳is˳a˳rather˳long˳bit˳of
		<-->text˳which˳changes
		<-->how˳it's˳displayed
		<-->==========================================
		"""
	)()

(() ->
	lLines = []
	log {
		fname: 'John',
		lname: 'Deighan',
		age: 68,
		home: 'Blacksburg, VA',
		}, {label: 'name', prefix: '<-->'}
	tester.equal {{LINE}}, lLines, """
		<-->------------------------------------------
		<-->name:
		<-->	---
		<-->	fname: John
		<-->	lname: Deighan
		<-->	age: 68
		<-->	home: Blacksburg,˳VA
		<-->------------------------------------------
		"""
	)()

(() ->
	lLines = []
	log [
		68,
		'a rather long string of text',
		{a:1, b:2}
		], {label: 'name', prefix: '<-->'}
	tester.equal {{LINE}}, lLines, """
		<-->------------------------------------------
		<-->name:
		<-->	---
		<-->	- 68
		<-->	- a˳rather˳long˳string˳of˳text
		<-->	-
		<-->		a: 1
		<-->		b: 2
		<-->------------------------------------------
		"""
	)()

# ---------------------------------------------------------------------------

simple.equal {{LINE}}, tamlStringify({a:"word", b:"blind"}), """
	---
	a: word
	b: blind
	"""
simple.equal {{LINE}}, stringify({a:"word", b:"blind"}), """
	---
	a: word
	b: blind
	"""

setStringifier JSON.stringify

simple.equal {{LINE}}, stringify({a:"word", b:"blind"}),
		'{"a":"word","b":"blind"}'

setStringifier tamlStringify

simple.equal {{LINE}}, stringify({a:"word", b:"blind"}), """
	---
	a: word
	b: blind
	"""

# ---------------------------------------------------------------------------

(() ->
	lItems = []
	setLogger (item) -> lItems.push(item)
	log 'a'
	log 'b'
	log 'c'
	setLogger console.log

	simple.equal {{LINE}}, lItems, ['a','b','c']
	)()

(() ->
	lItems = []
	setLogger (item) -> lItems.push(item)
	log 'a'
	log 'b'
	log 'c'
	setLogger console.log

	simple.equal {{LINE}}, lItems, ['a','b','c']
	)()

simple.fails {{LINE}}, () -> error("an error message")

# ---------------------------------------------------------------------------
