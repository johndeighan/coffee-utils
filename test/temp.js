// Generated by CoffeeScript 2.5.1
// temp.coffee
var main, max, total;

import {
  log
} from '@jdeighan/coffee-utils';

import {
  debug,
  setDebugging
} from '@jdeighan/coffee-utils/debug';

// ---------------------------------------------------------------------------
main = function() {
  var x;
  setDebugging(true);
  x = total([1, 2, 3]);
  return debug(`total = ${x}`);
};

// ---------------------------------------------------------------------------
total = function(lNumbers) {
  var i, len, n, sum;
  debug("enter total");
  sum = 0;
  for (i = 0, len = lNumbers.length; i < len; i++) {
    n = lNumbers[i];
    sum += n;
  }
  sum += max(lNumbers) + max(lNumbers);
  debug(`return ${sum} from total`);
  return sum;
};

// ---------------------------------------------------------------------------
max = function(lNumbers) {
  var i, len, m, n;
  debug("enter max()");
  m = lNumbers[0];
  for (i = 0, len = lNumbers.length; i < len; i++) {
    n = lNumbers[i];
    if (n > m) {
      m = n;
    }
  }
  debug(`return ${m} from max()`);
  return m;
};

// ---------------------------------------------------------------------------
main();
