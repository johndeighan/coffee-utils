import {error} from '@jdeighan/coffee-utils'
# log.test.cielo

import {undef, escapeStr} from '@jdeighan/coffee-utils'
import {arrayToBlock} from '@jdeighan/coffee-utils/block'
import {UnitTesterNorm, UnitTesterNoNorm} from '@jdeighan/unit-tester'
import {
	stringify, setStringifier, log, logItem, setLogger,
	tamlStringify, orderedStringify,
	} from '@jdeighan/coffee-utils/log'

simple = new UnitTesterNorm()

# ---------------------------------------------------------------------------

lLines = undef
setLogger (str) -> lLines.push(str)

# ---------------------------------------------------------------------------

class LogTester extends UnitTesterNoNorm

	transformValue: (lLines) ->
		return arrayToBlock(lLines)

tester = new LogTester()

# ---------------------------------------------------------------------------

(() ->
	lLines = []
	log 'enter myfunc'
	tester.equal 32, lLines, """
		enter myfunc
		"""
	)()

# ---------------------------------------------------------------------------

(() ->
	lLines = []
	log 'abc'
	log 'def'
	log 'ghi'
	tester.equal 44, lLines, """
		abc
		def
		ghi
		"""
	)()

# ---------------------------------------------------------------------------
# test logging various small objects

(() ->
	lLines = []
	log 'abc'
	logItem 'name', undef
	tester.equal 58, lLines, """
		abc
		name = undef
		"""
	)()

(() ->
	lLines = []
	log 'abc'
	logItem 'name', 42
	tester.equal 68, lLines, """
		abc
		name = 42
		"""
	)()

(() ->
	lLines = []
	log 'abc'
	logItem 'name', 'John'
	tester.equal 78, lLines, """
		abc
		name = 'John'
		"""
	)()

(() ->
	lLines = []
	log 'abc'
	logItem 'name', {a: 1, b: 'xyz'}
	tester.equal 88, lLines, """
		abc
		------------------------------------------
		name:
			---
			a: 1
			b: xyz
		------------------------------------------
		"""
	)()

(() ->
	lLines = []
	logItem 'name', ['a', 42, [1,2]]
	tester.equal 102, lLines, """
		------------------------------------------
		name:
			---
			- a
			- 42
			-
				- 1
				- 2
		------------------------------------------
		"""
	)()

# ---------------------------------------------------------------------------
# test logging various large objects

(() ->
	lLines = []
	log 'abc'
	logItem 'name', """
		This is a rather long bit of
		text which changes
		how it's displayed
		"""
	tester.equal 126, lLines, """
		abc
		name:
		==========================================
		This˳is˳a˳rather˳long˳bit˳of
		text˳which˳changes
		how˳it's˳displayed
		==========================================
		"""
	)()

(() ->
	lLines = []
	log 'abc'
	logItem 'name', {
		fname: 'John',
		lname: 'Deighan',
		age: 68,
		home: 'Blacksburg, VA',
		}
	tester.equal 146, lLines, """
		abc
		------------------------------------------
		name:
			---
			age: 68
			fname: John
			home: Blacksburg,˳VA
			lname: Deighan
		------------------------------------------
		"""
	)()

(() ->
	lLines = []
	log 'abc'
	logItem 'name', [
		68,
		'a rather long string of text',
		{a:1, b:2}
		]
	tester.equal 167, lLines, """
		abc
		------------------------------------------
		name:
			---
			- 68
			- a˳rather˳long˳string˳of˳text
			-
				a: 1
				b: 2
		------------------------------------------
		"""
	)()

# ---------------------------------------------------------------------------
# test ordered stringifier

(() ->
	setStringifier orderedStringify

	lLines = []
	log 'abc'
	logItem 'name', {
		fname: 'John',
		lname: 'Deighan',
		age: 68,
		home: 'Blacksburg, VA',
		}
	tester.equal 195, lLines, """
		abc
		------------------------------------------
		name:
			---
			age: 68
			fname: John
			home: Blacksburg,˳VA
			lname: Deighan
		------------------------------------------
		"""

	setStringifier tamlStringify    # reset
	)()

# ---------------------------------------------------------------------------
# test providing a prefix

(() ->
	lLines = []
	logItem 'name', undef, {prefix: '<-->'}
	tester.equal 216, lLines, """
		<-->name = undef
		"""
	)()

(() ->
	lLines = []
	logItem 'name', 42, {prefix: '<-->'}
	tester.equal 224, lLines, """
		<-->name = 42
		"""
	)()

(() ->
	lLines = []
	logItem 'name', 'John', {prefix: '<-->'}
	tester.equal 232, lLines, """
		<-->name = 'John'
		"""
	)()

(() ->
	lLines = []
	logItem 'name', {a: 1, b: 'xyz'}, {prefix: '<-->'}
	tester.equal 240, lLines, """
		<-->------------------------------------------
		<-->name:
		<-->	---
		<-->	a: 1
		<-->	b: xyz
		<-->------------------------------------------
		"""
	)()

(() ->
	lLines = []
	logItem 'name', ['a', 42, [1,2]], {prefix: '<-->'}
	tester.equal 253, lLines, """
		<-->------------------------------------------
		<-->name:
		<-->	---
		<-->	- a
		<-->	- 42
		<-->	-
		<-->		- 1
		<-->		- 2
		<-->------------------------------------------
		"""
	)()

(() ->
	lLines = []
	logItem 'name', """
		This is a rather long bit of
		text which changes
		how it's displayed
		""", {prefix: '<-->'}
	tester.equal 273, lLines, """
		<-->name:
		<-->==========================================
		<-->This˳is˳a˳rather˳long˳bit˳of
		<-->text˳which˳changes
		<-->how˳it's˳displayed
		<-->==========================================
		"""
	)()

(() ->
	lLines = []
	logItem 'name', {
		fname: 'John',
		lname: 'Deighan',
		age: 68,
		home: 'Blacksburg, VA',
		}, {prefix: '<-->'}
	tester.equal 291, lLines, """
		<-->------------------------------------------
		<-->name:
		<-->	---
		<-->	fname: John
		<-->	lname: Deighan
		<-->	age: 68
		<-->	home: Blacksburg,˳VA
		<-->------------------------------------------
		"""
	)()

(() ->
	lLines = []
	logItem 'name', [
		68,
		'a rather long string of text',
		{a:1, b:2}
		], {prefix: '<-->'}
	tester.equal 310, lLines, """
		<-->------------------------------------------
		<-->name:
		<-->	---
		<-->	- 68
		<-->	- a˳rather˳long˳string˳of˳text
		<-->	-
		<-->		a: 1
		<-->		b: 2
		<-->------------------------------------------
		"""
	)()

# ---------------------------------------------------------------------------

simple.equal 325, tamlStringify({a:"word", b:"blind"}), """
	---
	a: word
	b: blind
	"""
simple.equal 330, stringify({a:"word", b:"blind"}), """
	---
	a: word
	b: blind
	"""

setStringifier JSON.stringify

simple.equal 338, stringify({a:"word", b:"blind"}), '{"a":"word","b":"blind"}'

setStringifier tamlStringify

simple.equal 343, stringify({a:"word", b:"blind"}), """
	---
	a: word
	b: blind
	"""

# ---------------------------------------------------------------------------

(() ->
	lItems = []
	setLogger (item) -> lItems.push(item)
	log 'a'
	log 'b'
	log 'c'
	setLogger console.log

	simple.equal 359, lItems, ['a','b','c']
	)()

(() ->
	lItems = []
	setLogger (item) -> lItems.push(item)
	log 'a'
	log 'b'
	log 'c'
	setLogger console.log

	simple.equal 370, lItems, ['a','b','c']
	)()

simple.fails 373, () -> error("an error message")

# ---------------------------------------------------------------------------
